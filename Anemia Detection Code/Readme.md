# Anemia Detection Web App

See the [root README](../README.md) for the full write-up: how the 100% accuracy was diagnosed and fixed, current model performance, project structure, and how to run it.

Quick start:

```bash
python -m venv .venv && source .venv/bin/activate
pip install -r requirements-dev.txt
python train.py
python app.py
```
